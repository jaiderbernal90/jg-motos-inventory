import { StatusSalePipe } from './status-sale.pipe';

describe('StatusSalePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusSalePipe();
    expect(pipe).toBeTruthy();
  });
});
